import {useState, useEffect} from "react"

const ProfileTaskList = ({task_id}) => {
  const [tasks, setTasks] = useState([])

  return (
    <section>
      <ul>
        
      </ul>
    </section>
  )
};

export default ProfileTaskList;